# CLAUDE.md: anyBot

## Purpose
Local-first desktop workspace for named AI "employees" (a Grok-bot-style team UI). Each bot runs through a local harness (Claude Code, Codex CLI, Gemini CLI, Hermes, Cursor Agent CLI, or a custom CLI), can share project conversations, and can delegate to other bots. Source repo `gilfila/anyBot` (public since 2026-09-23, which made Actions free); binaries ship through the public `gilfila/anyBot-updates` feed. README.md is the features-first overview (screenshots in `docs/screenshots/`); `docs/operations.md` has the run/update/release/ops detail; design.md is the architecture/security doc.

## Stack
Electron 44 main process (`desktop/main.cjs`) + sandboxed React 19 renderer (`src/`, Vite) + coordinator in an Electron utility process (`runtime/`, SQLite). `electron-updater` for silent NSIS updates. Capacitor mobile companion (`mobile/`), headless server (`server/`).

## Run / Build / Test
- `npm start` (builds renderer, opens Electron). `npm run build` for the renderer only.
- `npm test` (node:test, ~270 tests), `npm run test:runtime` (Electron utility-process smoke), `npm run doctor`, `npm run verify:local` (all gates).
- `npm run package` builds `release/anyBot-Setup-X.Y.Z.exe` + `latest.yml` + blockmap. `.github/workflows/release.yml` validates PRs and publishes verified Windows assets after merge to main. Do not manually build/upload releases. One-time activation and recovery: `docs/releasing.md`.

## Structure
- `desktop/main.cjs`: window, tray, IPC, updater state machine. Renderer-callable coordinator methods must be in the `methods` allowlist (enforced by `tests/desktop-ipc.test.mjs`).
- `desktop/preload.cjs`: the only bridge (`window.anybot`). It exposes `runCommand` (a shell), so no untrusted content may ever run same-origin with the app.
- `runtime/coordinator.mjs`: commands, dispatch, delegation. Every command returns a snapshot; main decorates it with `update` + tray/login state (`withShellState`).
- `src/App.jsx`: whole app shell; `src/components/*`; `src/lib/markdown.js` (escape-first inline markdown, unit tested); `src/style.css`.

## Conventions
- **Design system "Studio paper"**: all colors are OKLCH tokens on `:root` in `src/style.css` (paper / ink / rule / accent vermilion / ok / warn / danger / block for dark code). Ink is the primary action color; vermilion only for unread, live work, send, updates. The "Studio paper layer" at the end of style.css overrides older rules. Do not reintroduce hex literals.
- Employee output is untrusted: escape before markdown, iframes for employee HTML use `srcdoc` + `sandbox="allow-scripts"` only (never `allow-same-origin` on local content).
- Each PR bumps `package.json` + `package-lock.json` version and adds a CHANGELOG entry.
- Do not change updater install behavior (`quitAndInstall(true, true)`, NSIS oneClick/perMachine=false) without testing a real update from the previous release.

## Last turn / Pending (2026-09-24)
**Roadmap in progress:** boards + doc pages + agent org/memory/knowledge graph. Plan: `~/.claude/plans/i-want-two-large-generic-liskov.md` (Phases 1–5). Decisions: agents act via `anybot-actions` blocks (MCP server later), the canvas is a Notion-style doc page, the chain of command is enforced, and knowledge is auto-derived plus agent-written.
**Phase 1 done (0.3.0): project boards.** `runtime/board.mjs` owns task data and agent permission rules. `runtime/actions.mjs` parses `anybot-actions` blocks. The coordinator handles `startTask`/`settleTask`/`autopilot`/`boardContext` and applies actions inside the run-completion transaction. UI lives in `src/components/board/*` (dnd-kit), with styles in `board.css` (tokens only). Verified by a real Electron e2e using a fake custom harness (`harnesses.json`) that emits actions.
**Phase 2 done (0.3.1): project doc page.** `runtime/docs.mjs` (block model, `doc.append`/`doc.section`, 20-version history), and `src/components/doc/ProjectDoc.jsx` shows rendered blocks until focused, then a textarea; `blocks.js` holds the pure editing rules and the conflict merge. Doc commands return doc objects (not snapshots), so the renderer calls them with `window.anybot.request` directly, not `act()`.
**Phase 3 done (0.3.2): chain of command + memory.** `runtime/org.mjs` (manager chain, loop checks, reports, stats), `runtime/memory.mjs` (scoped memories, FTS5 recall). The coordinator adds `applyAction` routing, `applyReview`, `onTaskMoved` (queues review runs, capped at 3 rounds), `orgContext` in the prompt (chain, unread reports marked read on delivery, recalled memories), and chain-of-command delegation in `delegate()`. UI: `src/components/org/*` (OrgPage with chart/panel/reports).
**Phase 4 done (0.3.3): knowledge graph.** `runtime/knowledge.mjs` derives workspace nodes and edges on read (never stored; ids `agent:`/`project:`/`task:`/`artifact:`). It stores written entities and edges (schema v11, FTS5 `kg_fts`) with provenance. Name resolution prefers workspace nodes in the run's own project. `recall()` feeds `knowledgeContext` in the prompt: pinned facts, then written facts near the assignment, then workspace edges touching named nodes. Pinning an edge drops its `[bot-written]` marker. `graph.ask` posts into (or creates) the bot's direct chat via `send()`. UI: `src/components/org/KnowledgeGraph.jsx` (d3-force SVG, Facts table, entity panel) + `knowledge.css`. Node colors use the `--cat-*` tokens.
**Released 0.3.3 (2026-09-23):** PR #28 merged as `f5c5f0a`, tag `v0.3.3` on the source repo, installer + blockmap + `latest.yml` published as Latest on `gilfila/anyBot-updates` (verified the public `latest.yml` serves 0.3.3). It is the first 0.3.x on the feed, so 0.2.27 installs jump straight to it. Confirmed on Tony's PC: the in-app update from 0.2.27 installed 0.3.3 and relaunched.
**CI (resolved 2026-09-23):** Actions was blocked by GitHub billing on the private repo; Tony made `gilfila/anyBot` public, so Actions minutes are free and CI runs again. Publishing still needs the one-time release GitHub App (`RELEASE_APP_CLIENT_ID` variable + `RELEASE_APP_PRIVATE_KEY` secret, `docs/releasing.md`).
**Packaging pitfall:** never run `npm run package` in a worktree whose `node_modules` is a junction. electron-builder then drops nearly every production dependency (the asar shipped with about 100 entries instead of about 5,600, and no `electron-updater`) and still exits 0. Package from a clean checkout with a real `npm ci` plus `node node_modules/electron/install.js`, then check the asar (`npx @electron/asar list`) for `electron-updater` and `builder-util-runtime` before publishing. `asar extract-file` writes into the current directory; run it in a scratch folder, never the repo root (it once overwrote `package.json` with the packaged, script-less copy).
**`three` in package.json:** it landed in `main` by accident with the 0.3.2 commit (it belongs to the avatar session). Nothing committed imports it yet. Leave it; the avatar work needs it.
**Diagnostics (0.3.5):** `desktop/diagnostics.cjs` owns `<userData>/logs/diagnostics.jsonl`, the only problem log. Record new failure paths through `coordinator.diagnostic({level, source, code, message, detail?, context?})` in the runtime, `diagnostics.record` in main, or `reportIssue` in the renderer. Add a title and hint for every new code in `describeIssue` (`src/lib/diagnostics.js`). Never put conversation text in entries. A daily scheduled Claude task (`anybot-diagnostics-triage`) reads the log.
**Canvas (0.3.6):** the Doc became the Canvas, available in every conversation as a tab and in the chat rail (`ProjectDoc` with `compact`). Code lives in `src/components/doc/` (`ProjectDoc.jsx`, `CanvasParts.jsx`, `blocks.js`, `canvas.css`). Block types add `table` (`rows`) and `link` (`url`). Files & outputs and chat links are derived from the snapshot, never stored. The runtime has its own GFM table parser in `runtime/docs.mjs` (runtime can't import `src/`).
**Themes (0.3.7):** themes are data in `src/themes/themes.js` (tokens, font, decor presets) with `validateTheme()` contrast rules. `src/lib/theme.js` applies them on `<html>` (inline tokens plus `data-backdrop`, `data-chrome`, `data-bubbles`, and `data-motion`). `ThemeBackdrop` draws the Matrix rain on a canvas; Solarpunk (`meadow`) and Cyberpunk (`grid`) play looping videos (0.3.11, `VideoScene`, media in `src/themes/media/`: VP9 WebM plus a first-frame JPEG used as poster, motion-off still, and theme-card preview). They were made with the Higgsfield MCP: a gpt_image_2_5 keyframe, animated by minimax_h3 with that keyframe as both start and end frame (flux_3_video failed on that input), then encoded with a 1s tail-to-head crossfade, one keyframe per loop, CRF 34, 1920x1080 (`scripts/make-scene-loop.sh`). Keep each clip around 5 MB; the `.scene-veil-*` gradients and the glass alphas carry readability. Presets are styled in `src/themes/themes.css`. Any new surface that should show the backdrop needs a transparent background under backdrop themes (the list in `themes.css`). Paper's data must mirror `:root` in `style.css` (tested). Custom themes are designed in `docs/themes.md` but not built (Tony's call).
**Approvals (0.3.8):** headless Claude runs can't show permission prompts, so anyBot sends them to the owner instead.
- **Bridge:** `runtime/approvals.mjs` (a loopback HTTP server with per-run tokens and a per-run MCP config in `<userData>/approvals/`) and `runtime/approval-mcp.mjs` (a stdio MCP bridge, listed in `build.asarUnpack` and run with `ELECTRON_RUN_AS_NODE`).
- **Harness flags:** set in `invocation()` in `adapters.mjs`. auto → `--permission-mode auto --allowedTools Edit(./**)`, plus `--permission-prompts host --permission-prompt-tool mcp__anybot__approve --mcp-config`. Never add `--strict-mcp-config`, because bots rely on the owner's other MCP servers.
- **UI:** `ApprovalBar` in the chat, and "Needs your approval" in the sidebar. Worker `attention` messages become Windows notifications in `main.cjs`.
- **Default mode:** auto. Schema v12 moved `ask` bots to `auto`.
**Bot editor + README (0.3.9):** avatars are sized per place with `<RobotAvatar size>` (0.3.11: sidebar 60, chat 64, roster 88, org cards 80 with `CARD_W`/`CARD_H` 236/144 in `OrgPage.jsx` matching `org.css`, agent panel 108); `.robot-avatar` CSS sizes are ignored. Dialog headings and the form's last `.primary.full` button are sticky (end of `style.css`). Model lists come live from each harness via `discoverLiveModels` in `adapters.mjs`, refreshed by the `harnesses.models` command each time the editor opens; don't reintroduce a hard-coded model filter. README screenshots are taken from the Playwright mock bridge (demo data, not a real profile); retake them when the UI changes noticeably.
**Chat bubbles (0.3.11):** every bot message has a solid bubble tinted per bot (`bubble` in the avatar JSON, palette in `src/lib/bubbles.js`). The hue is the bot's; the shade comes from `--bubble-*` on `:root` / `:root[data-scheme="dark"]` in `style.css`, mirrored in `BUBBLE_SHADES` and contrast-tested for every theme. New themes must pass `tests/bubbles.test.mjs`.
**Phone pairing by QR code (0.3.13):** Settings → Your phone (`src/components/PhoneLinkPanel.jsx`) → `runtime/phone-link.mjs`.
- **Transport:** the desktop dials out to a relay (`relay/`: Cloudflare Worker + Durable Objects; the same `room.mjs` runs on Node for tests). The phone app (`mobile/link-client.mjs`, `mobile/Scanner.jsx`) scans the QR and dials out too.
- **Crypto:** end-to-end in `runtime/link-protocol.mjs`; the relay only forwards opaque frames.
- **Routing:** phone requests go through `createMobileGateway().handle()` with a `linked` session. The HTTPS listener is optional (`serve: false`).
- **Relay URL:** the relay is deployed at `https://anybot-relay.anybot-desktop.workers.dev` on Tony's Cloudflare account (Wrangler is logged in on his PC). Redeploy with `npx wrangler@4 deploy --config relay/wrangler.toml` after changing `relay/`. `ANYBOT_RELAY_URL` overrides `DEFAULT_RELAY_URL`.
- **Testing the Worker:** run `ANYBOT_TEST_RELAY=... node --test tests/phone-link.test.mjs` against `wrangler dev --persist-to <short path>`. The default state path is too long on Windows and fails with "internal error".
- **Still missing:** there is no easy way to get the Android app onto a phone. Next is publishing a signed APK on the download page, with a "get the app" QR in the pairing dialog.
- **Reconnect races (0.3.20):** the relay often sees a new desktop or phone connection before the old one's close. The phone then gets `{"relay":"online"}` while it is already online. `rehandshake()` in `mobile/link-client.mjs` drops it to `connecting` until the new welcome arrives; this was the flaky "survives restarts" test. `relay/room.mjs` ignores sockets that are closing (`readyState >= 2`) and sends `left` only when the device has no live connection left. Local workerd already skipped closed sockets; the Node relay did not. **Pending on Tony:** redeploy the Worker for the `left` fix. Phone-side fixes ship with the next phone app build (the desktop installer doesn't include `mobile/` or `relay/`).
**Contrast (0.3.14):** over the animated backdrops, text must never sit on `--glass`; every text container gets an opaque `var(--paper)`/`var(--card)` box (`themes.css`, backdrop block), and the glass only fills empty space. `validateTheme` enforces 4.5:1 for every ink step on paper, card, paper-2, and paper-3. A new text surface in a backdrop theme needs a box.
**Sidebar attention icons (0.3.14):** `src/lib/attention.js` (`botAttention`: approval > error > question > pr > done) + `AttentionIcon.jsx`, shown right of the bot's name.
**Project edit/delete (0.3.17):** schema v14 adds `conversations.archived`. "Delete" archives, like bots (`conversations.setArchived`: cancels the project's runs, turns off autopilot and routines). New work into an archived project is refused through `requireOpenProject()` (in `send`, `startTask`, routine targets), and `dispatch()` cancels any queued run in an archived project. The sidebar project rows reuse the bot-row "…" menu (`FloatingMenu`, `openBotMenu` key `project:<id>`). `ProjectSettingsForm` is the Edit form (name, folders, Delete). Archived projects are hidden from the phone gateway and the knowledge graph.
**Handoffs always on (0.3.18):** every project (2+ bots) lets bots delegate and @mention teammates into threads; `Coordinator.isProject()` decides it from the member count. The `conversations.delegation` column is vestigial: the snapshot reports it from the member count for old clients, and payloads that set it are ignored.
**Terminal + queue (0.3.18):** `runtime/terminal.mjs`: `TerminalLog` (append-only `<userData>/terminal/<run>.log`, 4 MB cap, newest 300 kept) and `formatEvent(harness, event)` (stream-json events as terminal lines). `runHarness` takes `onTerminal` (redacted: the command, formatted events, raw text output, stderr, exit); the coordinator adds start/finish lines. `runs.terminal {id, offset}` returns new text + status; `RunTerminal.jsx` polls it every 700 ms while a run is live (Activity rows expand into it). Chat "Stop all" is `runs.stopConversation` (no pause); only Settings "Stop the team" (`runtime.stopAll`) pauses. Concurrency default is 8 (`snapshot.runtime.concurrency`). Metadata `pauseReset` marks the one-time 0.3.18 unpause. `activateMentions` queues a turn for a teammate that is still running (it skips only one already queued in the thread), so bots mentioned together can reply to each other. Paused state shows a banner in chat and Activity plus a sidebar Resume.
**Codex warnings (0.3.19):** `extractEvent(harness, value, state)` keeps per-run state; Codex `item.completed` items of type `error` are warnings before `turn.started` (config/MCP startup notices, e.g. an unrecognized `config.toml` key written by the Codex desktop app) and fatal after it. The terminal shows them as ⚠.
**Release notes (0.3.16):** the publisher uses the version's CHANGELOG entry as the feed's release notes, and the in-app update card shows them (`plainNotes`). Write CHANGELOG entries for users.
**Threads + @mentions (0.3.15):** schema v13 adds `messages.thread` and `runs.thread` (the root message id; NULL = top level).
- **Who works** is decided in `send()`: explicit `recipients` ∪ `mentionedIds(body, members)` (`runtime/mentions.mjs`, shared with the renderer). With neither, a direct chat goes to all members, a thread reply goes to its participants, and a project message is a note. Group runs get `thread = thread || messageId`; direct chats stay linear (thread NULL).
- **Everything a run posts** (notices, replies, hand-offs, delegated child runs) carries `run.thread`. `activateMentions()` starts teammates a bot @mentions (needs `conversation.delegation`, skips busy bots, capped by `MENTION_HOPS` since the last human message).
- **Prompt for a threaded run:** the thread in full, plus the 6 top-level messages before the root, each with its latest reply, as background.
- **UI:** `src/components/chat/` (`Composer` with the @ menu and To chips that toggle mentions, `ThreadPanel`/`ThreadSummary`, `threads.js`, `chat.css`). The composer places the caret in `useLayoutEffect` (a rAF lost keystrokes). The mobile app still sends explicit recipients and shows threads flat.
**Popups in scrolling lists:** use `FloatingMenu` (portal + fixed position), never an absolutely positioned child. That caused the clipped bot menu.
**Lean runtime M0 (0.3.21, branch `claude/lean-runtime-plan-wmf8pa`, PR open, not merged):** metrics only, no behavior change for bots.
- **Usage:** `runtime/usage.mjs` reads result events inside `runHarness` (`onUsage`, reported however the run ends); the coordinator stores OTel-named JSON in `runs.usage` (schema v15). Input tokens always include cached ones. Snapshot runs carry `usage` parsed. Activity shows per-run tokens and a "Tokens today" panel (`src/lib/usage.js`, `UsageToday.jsx`).
- **Prompt sections:** `Coordinator.promptParts()` returns `{text, sections}`; `prompt()` is its text (byte-identical to 0.3.19). `run_inputs` gains `sections` (sizes only), `hash`, `chars`. `boardContext()` now returns `{board, guide}`.
- **Retention:** `runtime/retention.mjs`: full prompt text for the newest 50 runs (`keepPrompts`), events 90 days (`eventDays`), both at startup; prompts also after each insert.
- **Corpus + baselines:** `scripts/corpus.mjs` (fixed seed; `direct`, `project2`, `project5k`), `make-corpus.mjs` (JSON gitignored, `manifest.json` committed and checked by `tests/corpus.test.mjs`), `bench-context.mjs`, `bench-sync.mjs`, `record-baseline.mjs` → `docs/architecture/baseline.json`. Changing the generator changes the manifest: regenerate and commit it.
- **Schema fixtures:** `tests/fixtures/schema-v14.sql` (0.3.19 Store output; `.gitignore` excludes `*.sql` except `tests/fixtures/schema-v*.sql`). Add a `schema-v15.sql` the same way when M2 bumps the schema.
- **Retention** runs through `Coordinator.retain()` (startup and after each stored prompt; events at most hourly). It swallows failures: retention must never stop a run.
- **Codex checkpoint done (2026-09-24, local session):** `docs/reviews/2026-09-24-M0-code.md`, 4 findings, all closed. `tests/fixtures/usage/codex.jsonl` is a real capture; it showed Codex joining its messages with no separator, now fixed in `extractEvent` (each `agent_message` after the first starts a paragraph). Gemini CLI 0.57.0 fails sign-in on Tony's PC (`IneligibleTierError`: Google dropped the CLI for individual Code Assist, so **Gemini bots don't work for Tony**), and Cursor Agent 2026.02.27 fails to start (`Cannot find module './240.index.js'`), so those fixtures stay documented shapes.
- **Running the Codex checkpoints:** `node scripts/adversarial-review.mjs design|code --milestone Mn [--base origin/main]` from Tony's PC (cloud sessions can't reach OpenAI). Code mode uses `codex exec` with the diff range; `codex review --base` refuses a custom prompt. It resolves the Codex desktop app's native binary (`%LOCALAPPDATA%\OpenAI\Codex\bin`), same as bots. Reports get repo-relative links (the repo is public).
- Main shipped 0.3.20 (phone reconnect races) first, so M0 is 0.3.21 and M1–M3 shift one release each.
- Also fixed: thread participants include bots whose turn failed (`threads.js`), and the composer's "who is/are in this thread".
**Lean runtime M1 (0.3.22, branch `claude/lean-m1-prompts`, PR #48 stacked on #47):** lean prompts.
- **`runtime/context.mjs`** (`buildContext`, pure) owns the prompt: stable layers, then history (`background`/`root`/`history`/`thread`), then per-turn (`board`/`org`/`knowledge`), then `assignment`. `Coordinator.promptParts()` only gathers rows. Budgets in `BUDGETS`, mirrored in `tests/budgets.json` (tested). Doc: `docs/architecture/context-budget.md`, ADR-0002.
- **History:** mandatory (root, assignment, mention, everything after the bot's last turn) is never cut; optional older messages fill ≤ 12,000 chars newest first. Other bots' long older messages keep head+tail unless the assignment names that bot (named ones get a 24,000 allowance first). Machine blocks are stripped from bot replies only. The assignment is sent once.
- **Reports** are marked read in the success transaction (`deliveredReports`), never while building the prompt.
- **Names are unique** among active bots (`uniqueName`), and `mentionedIds` ignores code (``` and ~~~ fences, inline, indented) and quoted lines.
- `npm run test:e2e` (`tests/electron-threads.cjs`): real utility process + fake harness via `harnesses.json`; in CI and `verify:local`.
- Result: project2 corpus median 46,264 → 14,896 (−68%), asserted in CI. Codex checkpoints: design 8 findings, code 3 findings, all fixed (`docs/reviews/2026-09-24-M1-*.md`).

**Lean runtime status (2026-09-24, local session):** #47 (M0, 0.3.21) CI green, waiting on Tony's merge. #48 (M1, 0.3.22, stacked on #47; merge #47 first) ready. It also carries the channel-background fix (queued thread runs no longer see a human reply posted after their assignment). #49 (M2, draft): harness session resume is built and tested but **off**. The live gate (`tests/live-sessions-smoke.mjs`) found no token saving: every Claude Code process writes ~31k tokens of its own context to the cache either way, and resumed Codex turns cost more. Tony decides whether to park it (recommended) or merge it as dormant (schema 16). **M3** design checkpoint done and §3.3 amended (1 critical, 7 high findings; the 64 KB gate is re-scoped to a summary response). Next is M3 step 1 on a branch from `claude/lean-m1-prompts` (or `main` once #48 merges): mutation replies `{epoch, seq, result}` with created ids, server read cursors, thread summaries, and mobile gateway scoped queries. Then M4 (Any Bot MCP server).
**Merging publishes:** `release.yml` publishes an installer to every install on each push to `main`, so a merge is a release. Never merge without Tony's OK. Two merges back to back: the first build's publish refuses (a newer merge exists) and only the last one ships, which is expected.
**One repo for releases (0.3.23):** releases (installer, blockmap, `latest.yml`) are published on `gilfila/anyBot` itself, which is the update feed from 0.3.23 on (`DEFAULT_UPDATE_FEED_URL` in `main.cjs`, `build.publish` in package.json; a test keeps them equal). `scripts/publish-release.mjs` also mirrors every release to `gilfila/anyBot-updates`, because installs before 0.3.23 have that feed built in. Drop the mirror (and the release GitHub App) only once no pre-0.3.23 installs remain. README has a Download link at the top pointing at `gilfila/anyBot/releases/latest`.
**Lean runtime outcome (2026-09-25):** #47 (M0) and #48 (M1) merged; 0.3.21 was superseded by 0.3.22 before it published. #49 (M2 session resume) closed and parked on `claude/lean-m2-sessions`. Tony wants more testing before any further performance work (M3/M4 on hold); focus moves to mobile. Gemini CLI uninstalled from Tony's PC (Google ended personal-account access on 2026-06-18, Pro included); `agy` (Antigravity CLI) works there and has a headless `-p --output-format stream-json` mode, a candidate harness.

**Next: the lean-runtime plan** (`docs/plans/lean-runtime.md`): M0 measure → M1 lean prompts → M2 harness session resume → M3 incremental sync → M4 Any Bot MCP server (was "Phase 5") → M5 A2A at the edge (only on request). Each milestone has a Codex CLI design checkpoint before code and a code checkpoint before merge (`node scripts/adversarial-review.mjs design|code --milestone Mn`; reports in `docs/reviews/`, every high finding fixed or rejected with evidence). ADRs live in `docs/adr/`. The plan, ADR-0001, the review prompts, and the script ride in the M0 PR (a docs-only PR would publish an empty update).
**Main checkout state (2026-09-24):** the main checkout (`claudeProjects/anyBot`) is parked on the merged `claude/approvals` branch with uncommitted drafts of the avatar (#29) and Any Bot rebrand (#36) work. Every hunk is already on `main` (checked file by file). Cleanup waits on Tony's OK (it discards those drafts); lean-runtime work happens in `.claude/worktrees/lean-runtime` instead. **Shared working tree (historical):** another session (Codex, avatar "Character Studio" redesign) edited these files in that checkout: `src/constants.js`, `src/lib/avatar-config.js`, `src/lib/bot-avatar-*.js`, `src/components/RobotAvatar.jsx`, `src/components/robot-avatar.css`, `.superdesign/`, and `design/bot-redesign-options.md`. It also has uncommitted hunks in `src/App.jsx`, `src/components/EmployeeForm.jsx`, and `CHANGELOG.md` (its Unreleased entry). Never stage those; for shared files, stage only your own hunks. Commit with explicit paths, not `git add -A src`.
**Local test caveat:** `tests/server.test.mjs` binds fixed port 4319. When another process holds it (for example a Codex preview server), 2 tests fail with EADDRINUSE; CI is unaffected.

### Earlier (2026-09-22)
**0.2.27: update-install fix.** Updating from 0.2.25 failed (NSIS exit 2): 0.2.25 shipped `@capacitor/android` Gradle output at 258-char install paths, and the old uninstaller's rollback move to `$PLUGINSDIR\old-install` exceeded MAX_PATH. `build/installer.nsh` `customInit` deletes that folder before the old uninstaller runs; `@capacitor` is excluded from `build.files`. Tested on Tony's broken 0.2.25 install with `--updated /S --force-run`: exit 0, relaunched, DB intact. 0.2.26 is on the feed but superseded; skip it.

**Full review + Studio paper redesign (0.2.26)**, merged as PR #23 and released: tag `v0.2.26` on the source repo, installer + `latest.yml` + blockmap published as `v0.2.26` (Latest) on `gilfila/anyBot-updates`. Fixed: `runs.dismiss` missing from IPC allowlist; update chrome flicker after actions; "Check now" wiping a downloaded update; unread-dot logic; blocked external links; broken HTML hand-off to the rail browser; `.user` CSS collision with `.message.user`; both CI workflows failing on every push (electron-builder auto-publish, setup-android package list). Security: same-origin sandboxed preview iframe could reach `window.anybot.runCommand`; markdown rendered raw employee HTML. UI: token sweep to OKLCH, live team roster, first-run hire flow, chat bubbles, sidebar status lines. Verified with `npm test` (127), `test:runtime`, doctor, a mocked-bridge Playwright pass, and a real-Electron e2e with an isolated profile. See CHANGELOG 0.2.26.

**Open:**
- HTML previews render but inline scripts stay blocked by the app CSP (inherited by srcdoc/blob frames). Running bot-built games/apps in-app needs a dedicated custom protocol with its own CSP.
- Sidebar bot rows are left-aligned (as shipped since the 0.2.22 CSS fix), not the right alignment requested in 0.2.23.
- The next release (0.2.28+) is the first real in-app update from a post-fix build; confirm it installs and relaunches.
