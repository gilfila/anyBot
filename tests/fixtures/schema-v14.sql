-- Any Bot workspace at schema 14 (0.3.19), generated by the 0.3.19 Store.
-- Synthetic data only. Used by tests/migrations.test.mjs.
CREATE TABLE metadata (key TEXT PRIMARY KEY, value TEXT NOT NULL);
CREATE TABLE employees (id TEXT PRIMARY KEY, name TEXT NOT NULL, role TEXT NOT NULL, harness TEXT NOT NULL, instructions TEXT NOT NULL, workspace TEXT NOT NULL, trusted INTEGER NOT NULL DEFAULT 0, created TEXT NOT NULL, archived INTEGER NOT NULL DEFAULT 0, revision INTEGER NOT NULL DEFAULT 1, model TEXT NOT NULL DEFAULT '', timeoutMinutes INTEGER NOT NULL DEFAULT 10, permissionMode TEXT NOT NULL DEFAULT 'dontAsk', avatar TEXT NOT NULL DEFAULT '', manager TEXT NOT NULL DEFAULT '');
CREATE TABLE conversations (id TEXT PRIMARY KEY, title TEXT NOT NULL, members TEXT NOT NULL, delegation INTEGER NOT NULL DEFAULT 0, created TEXT NOT NULL, allowedFolders TEXT NOT NULL DEFAULT '[]', artifactsFolder TEXT NOT NULL DEFAULT '', autopilot INTEGER NOT NULL DEFAULT 0, archived INTEGER NOT NULL DEFAULT 0);
CREATE TABLE messages (id TEXT PRIMARY KEY, conversation TEXT NOT NULL REFERENCES conversations(id), author TEXT NOT NULL, kind TEXT NOT NULL, body TEXT NOT NULL, created TEXT NOT NULL, thread TEXT);
CREATE TABLE runs (id TEXT PRIMARY KEY, conversation TEXT NOT NULL REFERENCES conversations(id), employee TEXT NOT NULL REFERENCES employees(id), message TEXT NOT NULL REFERENCES messages(id), parent TEXT REFERENCES runs(id), root TEXT NOT NULL, depth INTEGER NOT NULL DEFAULT 0, status TEXT NOT NULL, output TEXT NOT NULL DEFAULT '', error TEXT NOT NULL DEFAULT '', created TEXT NOT NULL, started TEXT, ended TEXT, dismissed INTEGER NOT NULL DEFAULT 0, task TEXT REFERENCES tasks(id), thread TEXT);
CREATE TABLE requests (key TEXT PRIMARY KEY, result TEXT NOT NULL);
CREATE TABLE run_responses (run TEXT PRIMARY KEY REFERENCES runs(id), message TEXT NOT NULL UNIQUE REFERENCES messages(id));
CREATE TABLE run_inputs (run TEXT PRIMARY KEY REFERENCES runs(id), prompt TEXT NOT NULL, created TEXT NOT NULL);
CREATE TABLE artifacts (id TEXT PRIMARY KEY, conversation TEXT NOT NULL REFERENCES conversations(id), run TEXT NOT NULL REFERENCES runs(id), name TEXT NOT NULL, blob TEXT NOT NULL, digest TEXT NOT NULL, bytes INTEGER NOT NULL, created TEXT NOT NULL);
CREATE TABLE routines (id TEXT PRIMARY KEY, name TEXT NOT NULL, conversation TEXT NOT NULL REFERENCES conversations(id), employee TEXT NOT NULL REFERENCES employees(id), prompt TEXT NOT NULL, minutes INTEGER NOT NULL, nextRun INTEGER NOT NULL, enabled INTEGER NOT NULL, created TEXT NOT NULL);
CREATE TABLE routine_occurrences (id TEXT PRIMARY KEY, routine TEXT NOT NULL REFERENCES routines(id), scheduled INTEGER NOT NULL, status TEXT NOT NULL, root TEXT REFERENCES runs(id), created TEXT NOT NULL, UNIQUE(routine,scheduled));
CREATE TABLE events (seq INTEGER PRIMARY KEY AUTOINCREMENT, type TEXT NOT NULL, payload TEXT NOT NULL, created TEXT NOT NULL);
CREATE INDEX run_status ON runs(status, created);
CREATE INDEX message_conversation ON messages(conversation, created);
CREATE TABLE tasks (id TEXT PRIMARY KEY, conversation TEXT NOT NULL REFERENCES conversations(id), title TEXT NOT NULL, description TEXT NOT NULL DEFAULT '', status TEXT NOT NULL CHECK (status IN ('backlog','in_progress','review','done')), priority TEXT NOT NULL DEFAULT 'none' CHECK (priority IN ('none','low','medium','high','urgent')), due TEXT NOT NULL DEFAULT '', labels TEXT NOT NULL DEFAULT '[]', assignees TEXT NOT NULL DEFAULT '[]', reviewer TEXT NOT NULL DEFAULT '', checklist TEXT NOT NULL DEFAULT '[]', parent TEXT REFERENCES tasks(id), sortKey REAL NOT NULL, createdBy TEXT NOT NULL, created TEXT NOT NULL, updated TEXT NOT NULL, revision INTEGER NOT NULL DEFAULT 1);
CREATE INDEX task_board ON tasks(conversation, status, sortKey);
CREATE TABLE task_activity (id TEXT PRIMARY KEY, task TEXT NOT NULL REFERENCES tasks(id), author TEXT NOT NULL, kind TEXT NOT NULL, body TEXT NOT NULL, run TEXT REFERENCES runs(id), created TEXT NOT NULL);
CREATE INDEX task_activity_task ON task_activity(task, created);
CREATE TABLE docs (conversation TEXT PRIMARY KEY REFERENCES conversations(id), blocks TEXT NOT NULL, revision INTEGER NOT NULL, updatedBy TEXT NOT NULL, updated TEXT NOT NULL);
CREATE TABLE doc_history (id TEXT PRIMARY KEY, conversation TEXT NOT NULL REFERENCES conversations(id), blocks TEXT NOT NULL, revision INTEGER NOT NULL, author TEXT NOT NULL, run TEXT, created TEXT NOT NULL);
CREATE INDEX doc_history_conversation ON doc_history(conversation, created);
CREATE TABLE memories (id TEXT PRIMARY KEY, employee TEXT NOT NULL REFERENCES employees(id), scope TEXT NOT NULL CHECK (scope IN ('private','team','project')), conversation TEXT NOT NULL DEFAULT '', body TEXT NOT NULL, tags TEXT NOT NULL DEFAULT '[]', pinned INTEGER NOT NULL DEFAULT 0, source TEXT NOT NULL, run TEXT, created TEXT NOT NULL, updated TEXT NOT NULL);
CREATE INDEX memory_employee ON memories(employee, updated);
CREATE VIRTUAL TABLE memories_fts USING fts5(body, tags, memory UNINDEXED);
CREATE TABLE reports (id TEXT PRIMARY KEY, fromEmployee TEXT NOT NULL REFERENCES employees(id), toEmployee TEXT NOT NULL, task TEXT, run TEXT, summary TEXT NOT NULL, read INTEGER NOT NULL DEFAULT 0, created TEXT NOT NULL);
CREATE INDEX report_inbox ON reports(toEmployee, read, created);
CREATE TABLE kg_entities (id TEXT PRIMARY KEY, type TEXT NOT NULL, label TEXT NOT NULL, labelKey TEXT NOT NULL, note TEXT NOT NULL DEFAULT '', source TEXT NOT NULL, createdBy TEXT NOT NULL, run TEXT, pinned INTEGER NOT NULL DEFAULT 0, created TEXT NOT NULL, updated TEXT NOT NULL, UNIQUE(type, labelKey));
CREATE TABLE kg_edges (id TEXT PRIMARY KEY, src TEXT NOT NULL, dst TEXT NOT NULL, relation TEXT NOT NULL, note TEXT NOT NULL DEFAULT '', source TEXT NOT NULL, createdBy TEXT NOT NULL, run TEXT, pinned INTEGER NOT NULL DEFAULT 0, created TEXT NOT NULL, updated TEXT NOT NULL, UNIQUE(src, dst, relation));
CREATE INDEX kg_edge_src ON kg_edges(src);
CREATE INDEX kg_edge_dst ON kg_edges(dst);
CREATE VIRTUAL TABLE kg_fts USING fts5(text, ref UNINDEXED);
CREATE TABLE approvals (id TEXT PRIMARY KEY, run TEXT NOT NULL, conversation TEXT NOT NULL, employee TEXT NOT NULL, tool TEXT NOT NULL, summary TEXT NOT NULL, detail TEXT NOT NULL DEFAULT '', status TEXT NOT NULL CHECK (status IN ('pending','approved','denied','expired','cancelled')), created TEXT NOT NULL, decided TEXT);
CREATE INDEX approvals_status ON approvals(status, created);
CREATE INDEX messages_thread ON messages(thread);
CREATE INDEX runs_thread ON runs(thread);
INSERT INTO metadata(key,value) VALUES ('schema','14');
INSERT INTO metadata(key,value) VALUES ('paused','false');
INSERT INTO employees(id,name,role,harness,instructions,workspace,trusted,created,archived,revision,model,timeoutMinutes,permissionMode,avatar,manager) VALUES ('e1','Builder','Engineer','codex','Build things','/tmp/ws',1,'2026-09-20T10:00:00.000Z',0,1,'',10,'dontAsk','','');
INSERT INTO conversations(id,title,members,delegation,created,allowedFolders,artifactsFolder,autopilot,archived) VALUES ('c1','Direct','["e1"]',0,'2026-09-20T10:00:00.000Z','[]','',0,0);
INSERT INTO messages(id,conversation,author,kind,body,created,thread) VALUES ('m1','c1','human','user','Synthetic assignment','2026-09-20T10:00:00.000Z',NULL);
INSERT INTO runs(id,conversation,employee,message,parent,root,depth,status,output,error,created,started,ended,dismissed,task,thread) VALUES ('r1','c1','e1','m1',NULL,'r1',0,'succeeded','Done','','2026-09-20T10:00:00.000Z','2026-09-20T10:00:00.000Z','2026-09-20T10:00:00.000Z',0,NULL,NULL);
INSERT INTO runs(id,conversation,employee,message,parent,root,depth,status,output,error,created,started,ended,dismissed,task,thread) VALUES ('r2','c1','e1','m1',NULL,'r2',0,'succeeded','Done','','2026-09-20T10:00:00.000Z','2026-09-20T10:00:00.000Z','2026-09-20T10:00:00.000Z',0,NULL,NULL);
INSERT INTO run_inputs(run,prompt,created) VALUES ('r1','Synthetic prompt one','2026-09-20T10:00:00.000Z');
INSERT INTO run_inputs(run,prompt,created) VALUES ('r2','Synthetic prompt number two','2026-09-20T10:00:00.000Z');
INSERT INTO events(seq,type,payload,created) VALUES (1,'message.accepted','{}','2026-01-01T00:00:00.000Z');
INSERT INTO events(seq,type,payload,created) VALUES (2,'run.completed','{}','2026-09-20T10:00:00.000Z');
